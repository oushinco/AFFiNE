apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: affine-claim0
  name: affine-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
